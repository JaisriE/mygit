body {
    margin: 0;
    padding: 0;
}
h1 {
    font-family: Georgia, serif;
}
button {
    font-family: Arial;
   
}
button:hover {
    background-color: black;
    color: white;
}
.main-header{
            text-align:center;
            padding:20px 0;
        }
        .main-header h1{margin:0;}
        .filter-table,.books-table{
            width:100%;
            border-collapse:collapse;
            background-color:#ffffff;
        }
        .filter-table td,
        .books-table th,
        .books-table td{
            padding:10px;
            border:1px solid #ccc;
            text-align:center;
            vertical-align:top;
        }
        .books-table th{
            background-color:#b7e4c7;
            font-size:18px;
        }
        .books-section{
            text-align:center;
            margin-top:10px;
        }
        .books-section h3{
            margin-bottom:10px;
            text-transform:uppercase;
            letter-spacing:1px;
        }
        .book-card{
            background:#f8fdf9;
            transition:0.3s;
        }
        .book-card img{
            display:block;
            margin:0 auto 10px auto;
        }
        .book-title{
            font-size:16px;
            margin:5px 0;
        }
        .btn-buy,.btn-cart,.btn-filter{
            padding:5px 10px;
            border:none;
            cursor:pointer;
            margin:3px;
            transition:0.2s;
        }
        .btn-buy{
            background-color:#2d6a4f;
            color:white;
        }
        .btn-cart{
            background-color:#40916c;
            color:white;
        }
        .btn-filter{
            background-color:#1b4332;
            color:white;
        }
        .btn-buy:hover,.btn-cart:hover,.btn-filter:hover{
            transform:translateY(-1px);
            opacity:0.9;
        }
        .book-card:hover{
            box-shadow:0 4px 10px rgba(0,0,0,0.15);
            transform:translateY(-3px);
        }
        .audio-section{
            text-align:center;
            padding:15px 0;
        }
        .footer{
            padding:15px;
            background-color:#ffffff;
        }
        .footer p{
            margin:5px 0;
        }